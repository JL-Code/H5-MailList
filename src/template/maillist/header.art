<div class="maillist__hd">
  <nav class="crumb-nav">
    <ul class="nav-list">
      {{each Navs}}
      <li class="nav__item {{if $value.active}}nav__item-active{{/if}}" data-id="{{ $value.id }}">
        {{ $value.text }}
      </li>
      {{/each}}
    </ul>
  </nav>
</div>